# Autonomous ISO-TP services

This directory contains various services, specifically ISO-TP related.

Use build commands separately (don't simply run make)
> make SOURCE_FILES=lcf_soh_reset.test.c
> make SOURCE_FILES=lcf_cells_reader.test.c
